Theme-route hardening task is obsolete.

Resolution:

1. The backend theme API was removed entirely.
2. `src/routes/themes.js` and `src/controllers/themeController.js` were deleted.
3. Route registration for `/api/themes` was removed from `src/routes/index.js`.
4. Theme persistence now lives in frontend JSON APIs in `jobs-app` and `marketplace-app`.

Status: Completed by removal/deprecation, no `requireRole` implementation needed for `/themes`.
