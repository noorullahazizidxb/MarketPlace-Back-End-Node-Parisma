Task plan for adding requireRole middleware and applying to /themes routes

1. Create a todo list entry via manage_todo_list (tool will be used next).
2. Implement middleware `requireRole` in `src/middleware/requireRole.js` that checks JWT (re-uses existing jwtAuth) and verifies roles.
3. Update `src/routes/themes.js` to use `requireRole(constants.roles.ADMIN)` for protected routes.
4. Update `marketplace.http` examples to include Authorization header for theme endpoints (if needed).
5. Run quick lint/type check (if available) and report results.

Notes:

- Assume tokens are signed with same scheme used by existing `jwtAuth` middleware. If `jwtAuth` already attaches `req.user` with payload, `requireRole` will use `req.user.roles`.
