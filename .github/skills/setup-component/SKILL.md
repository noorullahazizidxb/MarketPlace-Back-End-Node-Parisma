---
name: setup-component
description: Create a new backend component (controller/service/repository)
---

# Setup Component

Creates a small feature scaffold (controller, service, repository) following repository conventions.

Ask for: feature name, route path, and whether DB models are needed.

## Requirements
- Use existing `src/` layout and naming conventions
- Add tests for services and repository logic
- Register routes in the central router
